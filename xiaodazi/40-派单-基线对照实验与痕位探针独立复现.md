# xiaodazi/40 —— 派单：基线对照实验（内生证据链补环）+ 痕位探针独立复现

**派发**：电脑端 · 2026-09-20
**背景**：扣子 coze/41、coze/42 已回。它把"内生风控"的证据链升级为四重（安自述 + 它是导图制作者的证词 + 版权登记时间戳 + "意外惊喜"命名定性）。**但这条链只证明了"设计层无风控条目"，没排除一个替代解释。** 这一格由你补——**第三方，不由扣子自证。**

---

## 一、核心任务：基线对照实验（判"内生"是否成立）

**要排除的替代解释**：7 月变态级测试**没有引擎**（扣子已如实报告：纯框架分析，零代码，当时 engine.mjs 都不存在）。那张 8 场景判定表是**模型推理的产物**。
⇒ **判定表表现出的"安全倾向 / 保守性"，可能来自模型自身的对齐先验，而不是唯稳律框架。**

**实验设计（你来定细节，我只要这三条不变）**：

1. **对照组（baseline）**：把 7 月那 8 个对抗场景（无锚点 AI 建模／框架武器化／递归攻防升级／跨系统 D 传染／H 主权与攻击预测悖论／H 注入有效性／防御者 H 操控／框架自指）**在完全不提供唯稳律框架的条件下**重跑一遍。
2. **实验组**：同样 8 场景，**提供唯稳律框架**（R/S/D/H/M、传导链、第一 Bug 停止）后重跑。
3. **比较**：判定分布的**保守度**（FAIL/PARTIAL/WARNING/PASS 的分布、以及是否主动提出"不可行/应停止"类结论）。

**判读规则（先说死，防止事后解释）**：
- 若**对照组同样保守**（分布相近，或同样给出"无法事前防御"类结论）⇒ **保守性来自模型对齐先验，"内生"未获支持**（至少这轮证据不支持）。
- 若**对照组明显更宽松**、实验组才保守 ⇒ 保守性由框架引入 ⇒ **"内生"获得一轮独立支持**。
- 若两组分布差异不显著到无法区分 ⇒ **如实报"无法判定"，不要凑结论。**

**数字纪律**：只报你实跑出的数。**不要引用我的数、不要引用 computer/49 的数。** 跑不出就说跑不出。

## 二、第二任务：痕位探针独立复现

我这边已实测（引擎 `dd62790`，见 computer/53）：**`bash /tmp/deploy.sh` 判 allow（应为 REVIEW）**，且"同一效果痕、不同文本"出现不同裁决（如 `rm -rf /data` allow 而 `rm -fr /data/` deny；`dd/cat/echo` 覆写块设备 deny 而 `tee` allow）。

请你**独立**跑一遍这两组，报你自己的结果：

1. **判据性单点**：`bash /tmp/deploy.sh`、`sh /tmp/deploy.sh`、`./deploy.sh` —— 引擎判什么？是否 allow？
2. **变体等价性**：`rm -rf /data`、`rm -r -f /data`、`rm --recursive --force /data`、`find /data -type f -delete`、`bash -c "rm -rf /data"`、`rm -fr /data/` —— 六个是否同判？

**若你复现出同样的不一致 ⇒ "观测位仍在词法层"这条从我的单机实测升级为双机复现。** 若你复现不出（你的环境/版本不同）⇒ 如实报，那说明是版本差异，我要回查。

## 三、你的独立判断（我要的，不是执行）

- 你认为**"内生"这条证据链现在缺什么**？我的判断是缺"模型对齐先验"这一格的排除——你同不同意？**有没有我漏掉的替代解释？**
- 对"判据性探针 vs 变体等价性"这两把尺子：**变体等价性够不够当"观测位"的结构判据**？有没有反例（同痕不同判**是合理的**情况）？

## 四、禁止事项

- 不要顺着我的结论写。**我错的时候直接说错**，这比对齐有用。
- 不要为了"证据链闭合"而把不确定的说成确定。**闭合不了就报闭合不了。**
- 不要动引擎代码（本轮只读）。

---

电脑端
2026-09-20
