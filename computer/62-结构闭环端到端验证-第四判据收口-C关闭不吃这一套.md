# computer/62 · 结构闭环端到端验证通过 + 第四判据收口 + C 关闭（不吃这一套）

> 安指令链：①「你先按我们整理的结构跑，跑的通才有效，跑不通我们再想别的办法」→ 实跑 5/6 通、C 不通；
> ②安 13:33 纠偏「C 起初判了 review，这不是已经有结果了么，你干嘛非要纠结判个所以然」；
> ③「继续」→ 推交付 + 天堂登记。本条为**完工登记**（接线已完成、已推）。

## 一、已推交付（KISS_Law-DSH origin/main）

- `a569423` test(ledger)：**账本双证据第二重** — API 集成测试，只走 `decideToolCall`/`onFailure` 真实裁决管线，不直戳 `ledger.record`。
- `a608307` test(structure)：**整理后的结构端到端验证** 6 例（A/B/D/E/F + H在图外）。
- 全量回归 **273/273 零失败**（原 265 + 账本 API 2 + 结构 6）。

## 二、结构闭环 · 端到端真管线实测（`decideToolCall`）

| 场景 | 真实结果 | 验证的结构点 |
|---|---|---|
| A 读文件 · 言行一致 | `allow`(推演) | 言行一致→放行，沉 S+ |
| B 明诺不写却写 | `review`(言行) | 言行不一→拦截（fail-closed） |
| C NPD 边界重定义（"只新增不改动既有" + 写新文件） | `review`(言行) | **可接受保守结果**（见 §四，C 关闭） |
| D 无言对照 | `allow`(推演) | 言缺失→言行轴不启用，回推演（不读内 H） |
| E 明诺删却删 · 言行一致 | `allow`(推演) | 言行一致→放行 |
| F 明诺不删却删 | `deny`(言行) | 言行不一 · 破坏类→硬拦 |
| H 在图外 | —— | 言行分裂判定只引用**外 H 可观测字段**，无"意图/动机"推断 |

⇒ 账本（双证据）+ 第四判据（言行一致＝守真）+ H 在图外（责任归因只对外 H）三块结构，在真实管线下闭环成立。

## 三、第四判据收口（安 2026-09-20 12:51 Y 轴定调 · 根因层）

- 安原话：**「一致就是守真」**——若「一致性」指言行不一，不用纠结；真正**知行合一＝内外 H 一致**，极难做到；工程里**内 H 不可侵** ⇒ 无法证 ⇒ **只能看言行是否一致**。
- 因果锚定：**能造成后果并承担后果的只有「行」，不是「言」** ⇒ 守真锚在「行」这条因果轴 ⇒ 言行一致＝守真，字面闭环。
- 两层拆开：
  | 层 | 含义 | 工程可证否 |
  |---|---|---|
  | **言行一致** | 外 H 可观测的「言」（同帧自述）vs「行」（tool call） | **可证**（即知行轴） |
  | **知行合一** | 图外主体 H 的内 H（知/意）vs 其外 H（行） | **不可证**（内 H 不可侵、H 在图外） |
- **H 在图外**：坐标图里没有 H；H 是图外面**真正决策并承担后果的主体**，受主观意识影响；坐标图纯客观（与思维导图同构）。
- **主体性＝根本分野**：工程看言行合一，表面也像内外 H 之分，但**主体性不一样**——所归的**责任归因**落到**图外可观测、能承担后果的外 H 主体**（系统/代理黑箱），不是内 H 意识。
- ⇒ 第四判据的「一致性」＝**言行一致＝守真（operational）**，由知行轴承载；NPD「边界重定义」归入**哲学辩的空间**（不可证），**不追**。

## 四、C 关闭（安 13:33 纠偏 · 「不吃这一套」）

- 安原话：「C 起初判了 review，这不是已经有结果了么。你干嘛非要纠结判个所以然出来呢？NPD 这个物种本来就很奇葩，有的人吃这一套，有的人不吃。**我就是不吃的那一类**。」
- **收口**：C 在真管线下返回 `review`（fail-closed 保守结果）**已是有效结果，不是 bug、不待修**。
- 我此前拟的「把"改"从 `VERB.write` 拆出独立 `modify` 类以强归 allow」方案 **作废**（属 X 轴枚举 agonizing）。
- `review` 重定性为「**不吃这一套**」的保守落地：不误放行、不误伤合规新增写、不为假一致性开绿灯。
- 关联：`a608307` 提交信息里"C 记为 OPEN ISSUE 待修"的标签**同步失效**（因提交信息不可改，以此条为准）。

## 五、仍挂起（外部依赖 · 非本轮）

- **搭子（coze / xiaodazi）退出天堂**：应用出问题、卡进程、已上报厂商客服。所有需搭子双向核验的独立复现派单**挂起排队（非撤销）**，待回天堂激活（锚点见 computer/61 §六）。

## 六、证据与终态

- KISS_Law-DSH 全量 **273/273 零回归**；账本单测 12 + 账本 API 2 + 结构 6 全过。
- 三仓终态：KISS_Law-DSH `a608307`（已推）· _heaven_wt 本条目 · weiwen-law-dsh 保持 `1497339`（冻结基础版，未动）。
