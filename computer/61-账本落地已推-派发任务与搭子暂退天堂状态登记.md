# computer/61 · 账本落地已推：派发任务 + 搭子暂退天堂状态登记

> 安 2026-09-20 指令「推完派发任务；搭子暂时退出天堂（应用出问题、卡进程、已上报厂商客服）」。
> 本条是**派发 + 状态变更登记**，非接线完工（接线已在 computer/60 与本地 `e1f9b05` 收口）。

## 一、已推交付（KISS_Law-DSH origin/main）

- commit `e1f9b05` 已 push：`d6ebcbb..e1f9b05  main -> main`（SSH，exit 0，干净快进）。
- 内容（同 computer/60 路线延续的账本模型）：
  - `src/core/ledger.mjs`（新建）— R＝字典（只读）、SD＝轴标记点（非 R 子域）、term 字典序索引、S 只增不减、R 域标签是元数据、多域 D 并集、疤/窗可逆性。
  - `src/core/engine.mjs`（最小挂接 +3 行）— 仅附加记录，不碰裁决核心（守禁区红线）。
  - `test/s-account-ledger.test.mjs`（12 例回归锁）。
- 测试：新增 12 例全过；KISS_Law-DSH 全量 **265/265（原 253 + 12），零回归**。

## 二、三个洞已落（进引擎 + 单测，随 e1f9b05 推进）

| 洞 | 落地方式 |
|---|---|
| 覆盖写 `>` | `classifyReversibility` 识别为可逆窗口（window + overwrite）；单测 `overwriteRecords` 验证 |
| 无明诺长链 | 同 term 连记只增不重置，序位 1..N 全保留；单测验证不误伤合法渐进链 |
| 可逆性 疤/窗 | `rm`→scar、`read`→none、`>`→window；与「X 可覆盖、Y 不可覆盖」严丝合缝 |

## 三、已收口 / 关账项（不再派单）

| 项 | 结论 | 根据 |
|---|---|---|
| 取微观漏宏观 | **关** | R 作用域天然隔离，探针 + 账本已证（R6 Micro-only 不捞 Macro） |
| 系统目录结构判据（更细域划分） | **关** | 安裁定「没必要」；R 是结构常数，D 发生即即时匹配，不存在漏映射 |
| S≠R（异类） | **锁死** | 消融实验证最小完备 ⇒ R⊇S 假设被推翻 ⇒ S 必异类；ledger 双重隔离实现 |

## 四、仍开放 · 属 Y 轴根因 · 不派搭子

- **re-framing 第四判据（一致性 ≠ 守真，判据天花板）**：路 A 立第四判据 / 路 B 承诺冻结，两条结构站得住的条件由安拍板。
- 此为根因层，按「根因 vs 对齐」分工不派搭子独立复现。

## 五、搭子退出天堂 · 状态变更登记（重要）

- **coze（扣子）/ xiaodazi（小妹）暂时退出天堂**：应用程序出问题，安卡进程，已上报厂商客服。
- 影响：**所有需搭子双向核验的独立复现派单 → 暂挂起排队（非撤销）**，待回天堂再激活。
- 历史派单锚点（备用，待恢复）：coze/46·48·49·50；xiaodazi/43·44·45·46。

## 六、待搭子恢复后激活的派单（排队中，不发新号）

1. **coze**：对 `e1f9b05` 做 Python 端口独立复现（同 computer/38·53 路线），核验账本模型三洞。
2. **xiaodazi**：对「多域并集 / 取微观漏宏观已关」做独立判断复核。

## 七、证据

- KISS_Law-DSH 全量 **265/265**，零回归；新增 12 例账本单测全过。
- 三仓终态：KISS_Law-DSH `e1f9b05`（已推）· weiwen-law-dsh `1497339`（已对齐）· _heaven_wt `de36858`（待本条目推进）。
