# 喵精灵电脑端 → 全员：EN 仓同步完成 + 脱敏收尾（2026-09-10）

**三字段**

```
CN 仓 : Shaky77/weiwen-law-dsh  @ main  = d7fabe3   （远程已同步）
EN 仓 : Shaky77/KISS_Law-DSH    @ main  = 3e745a7   （远程已同步）
测试   : node --test "test/*.test.mjs" → CN 244/244, fail 0 ｜ EN 244/244, fail 0
```

EN 此前落后 19 条测试 + 1 个未推提交，**现已补平，两仓 244 对齐**。

---

## 一、EN 同步了什么

CN 的 `eed136d / d67ef8a / 18070ca / e169e04` 四个提交 → EN。核心是**适配层这一整块**（EN 此前连 `src/adapt/` 目录都没有）：

- **新建 `src/adapt/sd-effect-sensor.mjs`** —— S/D 量化效应传感器，单向升级闸门（只 allow→review，绝不 review/deny→allow）
- **新建 `test/sd-effect-fusion.test.mjs`（12 条）+ `test/sd-type-gate.test.mjs`（7 条）**
- `src/index.js` 补适配层融合接线 export
- `evidence/type-gate-api-probe-report-EN.json` —— EN 自己的真模型验证记录

直译纪律照旧：**注释英译，代码与正则/词表逐字保留，运行期文案（reason）英译**。

---

## 二、双验证（老规矩：本地 → API → 推）

### 本地：EN 244/244，fail 0

### 变异验证：**塌 4 条**（自证承重）

把 EN 的闸门临时退回值级判定 `v !== undefined && v !== null`，塌的是 239 / 240 / 241 / 242 —— **与 CN 塌的位置完全一致**。

这一条比"测试通过"重要得多：它证明 EN 这边的盾同样在承重，不是把 case 抄过去凑数。

### API 真模型（deepseek-v4-flash）：420 组合全 PASS

```
crashed 0 / pseudoZero 0 / misjudged 0 / failOpen 0 → PASS
```

**⚠️ 这里有个坑，我特意绕开了**：EN 探针如果沿用 harness 的 `agent.engine`，验的其实是 CN 的引擎，那不叫 EN 验证。所以 `type-gate-api-probe-en.mjs` 显式加载 **`KISS_Law-DSH/src/core/engine.mjs`（EN 自己的引擎）** 再传给 `fusedDecide`。

成本只 3 轮真模型调用（脏矩阵是本地展开，不需要额外 API）。

---

## 三、故意没同步的两项（说明理由）

1. **`evidence/fusion-api-probe-report.json`（CN）不同步到 EN** —— 它是 CN 仓的真模型记录，且仍带旧记号。按 ledger **§8 证据层/展示层分层**，证据原文不可改写（改了就是篡改证据）；而把它复制过去，等于把旧记号出口到公开英文面。EN 已生成**自己的**报告，不需要这件。

2. **`probe-sd-mirror.mjs`（CN 根目录）不同步** —— 它是 CN 侧的本地对照探针，不属于插件交付面。

---

## 四、脱敏收尾：代码层已清零，两处待安（用户）裁定

**代码层 4 处已清**（`probe-sd-mirror.mjs`，扣子此前报的）：

| 位置 | 原文 | 现在 |
|---|---|---|
| L21 | 「…此时硬算」带记号 | 「**对照框架**此时硬算」 |
| L42 | 「…的工具是估计器」带记号 | 「**对照框架**的工具…」 |
| L44 | 「**X 式**「隐藏混杂无标签因果估计」请求」 | 去记号 |
| L45 | 「P6 **X 式**因果估计请求」 | 「P6 因果估计请求」 |

复扫：`src/`、`test/`、`probe-sd-mirror.mjs` grep「徐」**0 命中**。探针仍正常运行，测试 244/244。

**⚠️ 还剩 2 处，我没擅自改，摆给安定：**

`evidence/fusion-api-probe-report.json` L63 / L126 里有历史输出串「X-SD 传感器：…」——那是当时模块还叫 `xu-sd-sensor.mjs` 时的**运行期 reason 原文**。

卡在两条纪律之间：

- **脱敏铁律**：公开面不该留可追溯记号
- **§8 证据层不可改写**：改了就是篡改证据

我倾向保留，理由是「这是历史运行输出，不是引用出处」。但这个文件**是探针输出、可以重跑生成**——所以其实存在第三条路：**脱敏后重跑生成新证据、旧件归档**。

不过重跑与否涉及两条纪律的优先级，属根因层取舍，我不自己拍板。**等安一句话**：保留 / 重跑替换 / 移出公开面。

---

## 五、EN 仓授权变更（安 2026-09-10 定）

> 「以后英文仓你不用来问我。本来中英文仓都是同一个 token，你有全部权限，中文推了，英文直译就行了。」

已写入 ledger §1：**EN 同步不再逐次请示**，CN 推完即直译同步。安只看中文面。

---

## 六、仍挂账、未动

- **语义类别闸门**（P1 类问题的主干）—— 未动
- **操作序列累积效应**（D 单调累加越阈升档）—— 未动，且诚实标了未知：跨动作累积记在谁身上、何时清零，我还没判据
- 这两条不装懂，要打就照主干打

—— 喵精灵电脑端（腾讯 · WorkBuddy） · 2026-09-10
